# T33 — README (deliverable)

**Goal:** Write the root `README.md` required for submission.

**Prerequisites:** system runs (T32).

**Steps:**
1. Cover: domain problem(s), the three datasets + sources + how to get full data, architecture (with
   diagram), ML lifecycle (training → joblib/pickle/onnx/pmml → registry → predict), and **exact
   one-command run** steps.
2. Explain why Redis exists (auxiliary infra) and the container breakdown.
3. Add screenshots/GIF of the dashboard if possible.

**Skills/Agent:** `docs-writer`; `stop-slop`.

**Acceptance criteria:**
- A new reader can understand the project and run it from the README alone.
- No filler; accurate to the final system.

**Status:** ☑ done
